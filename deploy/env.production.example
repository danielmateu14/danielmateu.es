# Copiar a /home/ubuntu/danielmateu.es/.env en la VM y rellenar.
# NO subir este archivo con datos reales al repositorio.

# Genera una nueva con:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=pon-aqui-una-clave-larga-y-secreta

DEBUG=False

ALLOWED_HOSTS=danielmateu.es,www.danielmateu.es

# Postgres LOCAL de la VM (sin SSL). Ajusta usuario/clave/nombre a los que crees.
DATABASE_URL=postgres://danieldb:TU_PASSWORD@127.0.0.1:5432/danielmateu

# Deja USE_SQLITE sin definir (o False) en producción.
# USE_SQLITE=False
